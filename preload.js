// Kept minimal: nodeIntegration is enabled for this local personal tool, so the
// renderer uses ipcRenderer directly. This file exists so the build stays stable
// if contextIsolation is turned back on later.
